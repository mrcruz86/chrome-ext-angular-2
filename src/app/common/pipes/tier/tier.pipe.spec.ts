/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TierPipe } from './tier.pipe';

describe('TierPipe', () => {
  it('create an instance', () => {
    let pipe = new TierPipe();
    expect(pipe).toBeTruthy();
  });
});
