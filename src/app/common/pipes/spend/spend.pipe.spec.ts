/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SpendPipe } from './spend.pipe';

describe('SpendPipe', () => {
  it('create an instance', () => {
    let pipe = new SpendPipe();
    expect(pipe).toBeTruthy();
  });
});
